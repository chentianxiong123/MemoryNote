{
  "name": "core_brain",
  "version": "0.10.0",
  "private": true,
  "description": "Runtime dependencies for core bundled hooks",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "dependencies": { },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  }
}
